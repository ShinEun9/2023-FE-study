describe("자스민 테스트 묶음", () => {
  // it 함수 : 함수 테스트 유닛. 첫번째 인자로 테스트할 함수에 대한 설명, 두번째로 테스트할 코드를 전달
  it("인자를 전달하면 1을 더해주는 함수", function () {
    let num = 20;
    // expect: 기대식. 실행할 함수의 결과값을 인수로 전달
    // toBe: 매처함수. 내가 기대한 결과와 일치하는지 판단하는 함수
    expect(plusOne(num)).toBe(num + 1);
  });
});
